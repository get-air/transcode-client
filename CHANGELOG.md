# Changelog

## Unreleased

## 0.1.0

- Add Promise and Effect clients for source registration, probing, relay access, session lifecycle, and playback warming.
- Add an explicit `@get-air/video` transcode adapter with browser-safe HLS playback, track switching, and a configurable startup reserve.
- Add a transcode-only browser demo with session, route, buffering, latency, and request-budget diagnostics.

